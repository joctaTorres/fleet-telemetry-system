# AI Build Logs — Session Index

Append-only log of AI build sessions. One line per session (id, name, brief). Newest
entries go at the bottom. See each session's report in this directory.

| Session id | Name | Brief |
| --- | --- | --- |
| 8b137e8-20260616182352 | propose-standard — fleet telemetry monitoring architecture | Authored `telemetry-architecture` standard: CDC propagation, primary/replica split, DB-enforced concurrency control. |
| 73c568d0-20260616 | propose — telemetry-persistence | Proposed root change: schema (`raw_events`, `vehicle_current_state`) + idempotent upsert write path and `GROUP BY` fleet aggregate; features + plan, no implementation. |
| 08ddfba8-20260616 | apply — telemetry-persistence | Implemented persistence layer: migrations, `persist_telemetry` (atomic raw-append + ON CONFLICT upsert), `aggregate_fleet_state` (`GROUP BY`), env DSN, docker-compose.test.yml; integration suite passes (6/6, exit 0). |
| 08ddfba8-20260616 | apply — ingestion-post-endpoint | Added stateless ingestion FastAPI app with `POST /telemetry`: validates `TelemetryEvent` (422 on invalid), calls `persist_telemetry`, returns 201; added `httpx` dev dep + `TestClient` integration tests; suite passes (15/15, exit 0). |
| 08ddfba8-20260616 | apply — fleet-state-endpoint | Added frontend FastAPI app with `GET /fleet/state`: returns `aggregate_fleet_state()` per-status counts (zero-filled) as JSON; added GET tests + cross-API proof-of-work (50 vehicles concurrent ingest → exact aggregate); suite passes (18/18, exit 0). |
| 08ddfba8-20260616 | apply — zone-counts-increment | Added `zone_counts` table + idempotent ~20-zone seed, `zone_entered` event field, single server-side atomic `UPDATE ... entry_count + 1 WHERE zone_id` inside the `persist_telemetry` transaction, and `zone_entry_counts()` read seam; 50 concurrent entries → exact count, no lost updates; suite passes (22/22, exit 0). |
| 08ddfba8-20260616 | apply — zone-counts-endpoint | Added `GET /zones/counts` to the frontend API as a thin adapter over `zone_entry_counts()` (live per-zone totals, all ~20 zones zero-filled); landed phase proof-of-work `test_zone_counts.py` (50 concurrent ingest → exact count via GET, null-zone no-op, all zones present); completes the `zone-traversal-counter` end-to-end slice; suite passes (27/27, exit 0). |
| 08ddfba8-20260616 | apply — anomaly-detection | Root change of the anomaly phase: `anomalies` table (indexed on `(vehicle_id, detected_at)`), event/`vehicle_current_state` extended with `speed_mps`/`error_codes`/`pos_x`/`pos_y`, and synchronous in-transaction detection wired into `persist_telemetry` (stateless: fault/error_codes/low_battery/overspeed; stateful vs prior: stuck/teleport/battery_rising) writing one row per fired rule, plus the `recent_anomalies(vehicle_id, since, until)` indexed read seam; `GET /anomalies` + comms-loss watchdog are follow-ons; suite passes (46/46, exit 0). |
| 08ddfba8-20260616 | apply — anomaly-query-endpoint | Added `GET /anomalies` to the frontend API as a thin adapter over `recent_anomalies(vehicle_id, since, until)` (required `vehicle_id` + inclusive `[since, until]` ISO timestamp range → JSON list ordered by `detected_at`, empty list when none), an indexed read over `(vehicle_id, detected_at)`; landed phase proof-of-work `test_anomalies.py` (each default anomaly class fires exactly at its threshold via the ingestion write path → GET read, end-to-end reachability, window inclusion/exclusion with inclusive bounds and vehicle filtering); completes the phase's end-to-end slice (comms-loss gap is the `comms-loss-watchdog` follow-on); suite passes (57/57, exit 0). |
| 08ddfba8-20260616 | apply — fault-transition-core | Root change of the fault-transition phase: `vehicles` (FOR UPDATE lock anchor + authoritative status), `missions` (partial unique `uq_active_mission_per_vehicle`), and `maintenance_records` (partial unique `uq_open_maintenance_per_vehicle` idempotency backstop) tables, plus `transition_to_fault(vehicle_id, reason)` — one `conn.transaction()` taking `SELECT ... FOR UPDATE`, returning `False` if already fault (transition guard), else cancelling the active mission (`RETURNING mission_id`), inserting one maintenance record `ON CONFLICT DO NOTHING`, flipping status to fault, returning `True`; concurrent/duplicate faults settle to exactly one cancelled mission + one record. HTTP status-update op + phase proof `test_fault_transition.py` are the `status-update-endpoint` follow-on; slice suite 5/5, suite passes (66/66, exit 0). |
| 08ddfba8-20260616 | apply — comms-loss-watchdog | Added the by-absence comms-loss rule completing the anomaly phase: `COMMS_LOSS_TIMEOUT_SECONDS = 5`, a `detect_comms_loss(now)` sweep seam (single idempotent `INSERT ... SELECT` flagging vehicles whose last `recorded_at` is strictly older than `now - 5s` and lacking a `comms_loss` at/after that reading — once per silence episode, re-arming after recovery), and a thin standalone `app/watchdog.py` loop process kept separate from the stateless ingestion API; `test_comms_loss.py` (strict boundary, once-per-episode, re-flag) + appended comms-loss-gap scenario to the phase proof `test_anomalies.py` (POST → sweep past timeout → `GET /anomalies` returns `comms_loss`, within-timeout not flagged); phase proof 12/12, suite passes (61/61, exit 0). |
| 08ddfba8-20260616 | apply — status-update-endpoint | Added `POST /vehicles/{vehicle_id}/status` to the stateless ingestion (write) API as a thin adapter over the proven `transition_to_fault` seam: a `fault` update delegates to that one row-locked, idempotent transaction (no new transaction logic), non-fault statuses take a guarded `set_vehicle_status` UPDATE, `LookupError`/missing row → 404, Pydantic `VehicleStatusUpdate` (`extra=forbid`) → 422; 200 returns `{vehicle_id, status, applied}` reporting whether the call changed state or was an idempotent no-op; landed phase proof-of-work `test_fault_transition.py` (single, duplicate-sequential, 20 concurrent → exactly one cancelled mission + one maintenance record + status fault + one `applied:true`, unknown→404, bad-status→422); completes the `fault-transition` end-to-end slice; suite passes (71/71, exit 0). |
| 08ddfba8-20260616 | apply — websocket-fanout | Root change of the `realtime-cdc-websocket` phase (fan-out half): added Redis (`redis>=8.0.0`, `get_redis_url()` from env, `redis:7-alpine` in compose) and a new `app/events.py` channel contract (`fleet:events` + the three delta types + `snapshot`). Made the frontend API stateful without authoritative data: a `ConnectionRegistry` (lock-guarded WS set with `broadcast()` that drops dead sends), one async Redis subscriber started in a FastAPI `lifespan` that forwards each published message verbatim, and `@app.websocket("/ws")` that sends a one-shot snapshot (fresh from `aggregate_fleet_state()` + `zone_entry_counts()`) then streams deltas; REST handlers unchanged, ingestion touches no Redis. Proven at the Redis seam (test publishes patches directly, standing in for the not-yet-built CDC consumer) by `test_ws_fanout.py` (snapshot-on-connect, sub-second delivery, all three types verbatim, two-client fan-out, no-message-when-idle, dead-client drop); replica + CDC consumer + phase proof `test_realtime_ws.py` are the `cdc-consumer` follow-on; suite passes (77/77, exit 0). |
| 08ddfba8-20260616 | apply — cdc-consumer | CDC source half of the `realtime-cdc-websocket` phase, landing its full blackbox proof: primary runs `wal_level=logical`; added a streaming read **replica** (pg_basebackup bootstrap) and a singleton **`cdc`** service (`app/cdc_consumer.py`) tailing the logical slot `fleet_cdc` via `pg_logical_slot_get_changes` (`test_decoding`, since `wal2json` is absent from alpine), translating the three watched tables to the three event types and publishing JSON to `fleet:events` — the only producer; ingestion still touches no Redis. Frontend connect snapshot moved to the replica (`get_replica_dsn()`, `replica_connection`, `conn_factory` read seams), closing the "snapshot from primary" deviation. New phase proof `test_realtime_ws.py` drives the real stack with no Redis in the test: fault `POST → vehicle_state_changed`=fault sub-second, zone-entry `POST → zone_count_changed`, overspeed `POST → anomaly_detected`, a rolled-back write surfaces no patch (vs a committed sentinel), and the snapshot is served from the standby (`pg_is_in_recovery`); `_clean_tables` made CDC-aware (reset only non-zero counters, drain slot before subscribe). Proof exit 0 (5 passed), suite passes (82/82). |
| 08ddfba8-20260616 | apply — read-replica-split | Read-replica half of the `realtime-cdc-websocket` phase, carved out of the parked/re-split `cdc-consumer`: a streaming physical read **replica** (`docker/replica-entrypoint.sh` bootstraps via `pg_basebackup --wal-method=stream --write-recovery-conf`, runs as a hot standby; primary set up for replication via `wal_level=logical` + `docker/primary-init` `pg_hba` rule) and a separate replica connection pool (`get_replica_dsn()`/`get_replica_pool()`/`replica_connection()`, `close_pool()` tears down both). Moved the **entire** frontend read surface to the standby — connect snapshot + `GET /fleet/state`/`/zones/counts`/`/anomalies` via a `conn_factory` read-seam param (now also on `recent_anomalies`) — closing the documented "snapshot from primary" deviation; the write path is untouched and stays on the primary. Removed the out-of-scope CDC bits that the parked attempt left behind (the `cdc` compose service, the dangling `app.cdc_consumer` import + logical-slot machinery in conftest), per this slice's "no CDC-slot dependency" scope. Proof `test_replica_snapshot.py` (5/5): standby `pg_is_in_recovery` + streamed schema, committed write reflected via replica read seams + REST, rolled-back write never visible, standby rejects writes (`ReadOnlySqlTransaction`), reads via replica pool / writes via primary. CDC source + full phase proof `test_realtime_ws.py` are the `cdc-pgoutput-translate`/`cdc-end-to-end-ws` follow-ons; suite passes (82/82, exit 0). |
| 08ddfba8-20260616 | apply — cdc-pgoutput-translate | CDC source half of the `realtime-cdc-websocket` phase: the singleton consumer that supplies the producer for the `fleet:events` channel. Added `app/cdc.py` (publication/slot names + watched-table → event-type map), migration `0009` (idempotent `CREATE PUBLICATION fleet_events_pub` over exactly `vehicle_current_state`/`anomalies`/`zone_counts` + `REPLICA IDENTITY FULL` on the two UPDATE tables), and `app/cdc_consumer.py` — a singleton that opens a `replication=database` connection via psycopg's raw `pq` layer (no new dep), runs `START_REPLICATION SLOT fleet_cdc_slot LOGICAL … (proto_version '1', publication_names 'fleet_events_pub')`, hand-decodes the binary pgoutput Begin/Relation/Insert/Update/Commit messages (only committed txns emit), translates each watched table to its `app.events` type, publishes JSON to Redis (sole producer; ingestion still touches no Redis), and sends standby LSN feedback so the slot's confirmed-flush advances. `cdc_stream` conftest fixture runs the consumer in-process on a fresh-per-test slot (created after `_clean_tables` so cleanup writes aren't decoded). Proof `test_cdc_translate.py` (7/7): one event per committed upsert/anomaly/zone-increment with matching payload, correct contract `type`, rolled-back write → no event (+ committed sentinel), non-watched `raw_events` write → no event, publication members are exactly the three tables. Full phase proof `test_realtime_ws.py` is the `cdc-end-to-end-ws` follow-on; suite passes (89/89, exit 0). |
| 08ddfba8-20260616 | apply — cdc-end-to-end-ws | Terminal slice of the `realtime-cdc-websocket` phase: runs the proven `CdcConsumer` as a long-lived singleton **`cdc`** compose service and lands the phase blackbox proof of the whole real-time path. Added a process supervisor to `app/cdc_consumer.py` — `run_forever()` loops `ensure_slot()` + `CdcConsumer().run(stop)` with bounded backoff (`0.5→10s`), `main()`/`python -m app.cdc_consumer`, and SIGTERM/SIGINT → clean stop (final standby feedback, slot not wedged) — touching no decode/translate/publish logic. Key fix: `_publication_exists()` gates slot creation, since the service starts before migrations and anchoring the slot ahead of the publication wedges decoding on pre-publication WAL (`publication … does not exist`); the supervisor waits, then streams. New `cdc` service in `docker-compose.test.yml` (api image, env DSN/URL, `depends_on` db+redis healthy, single reader); api config unchanged. Proof helpers in conftest (`wait_cdc_service_streaming`, `wait_frontend_subscribed`, `WsReader` single-thread WS drain); `cdc_stream` left intact but unused. Phase proof `test_realtime_ws.py` (5/5) on the real primary+replica+Redis+`cdc`: fault `POST → vehicle_state_changed`=fault sub-second, zone entry → `zone_count_changed`, low_battery → `anomaly_detected`, rolled-back write → no event (+ committed sentinel), CDC is sole producer (write path has zero Redis refs, clean POST → exactly one delta). Cold-start proof exit 0; full suite minus slot-exclusive `test_cdc_translate.py` 87 passed; that file 7/7 in isolation. |
